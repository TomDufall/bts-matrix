Automated testing here is implemented using pytest.
To use it, first install pytest (OS cmd line: 'pip install pytest')
Then, run it by running 'pytest' [optional test file, if not given it will run all test files it finds]
Note: pytest is run from the OS cmd line, not a python prompt